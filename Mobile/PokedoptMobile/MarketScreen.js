import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

export const MarketScreen = () => {
  return(<View ><Text>AMRKET</Text></View>)
};
